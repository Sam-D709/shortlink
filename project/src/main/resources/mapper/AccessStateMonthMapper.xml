<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.samd.shortlink.project.dao.mapper.AccessStateMonthMapper">

    <insert id="batchInsertOrUpdate">
        INSERT INTO accessstatemonth (fullshorturl, pv, uv, month, year, delflag)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.fullshorturl}, #{item.pv}, #{item.uv}, #{item.month}, #{item.year}, 0)
        </foreach>
        ON DUPLICATE KEY UPDATE
        pv = pv + VALUES(pv),
        uv = uv + VALUES(uv)
    </insert>

</mapper>